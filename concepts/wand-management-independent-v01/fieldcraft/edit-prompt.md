Use case: precise-object-edit
Asset type: targeted correction to an existing Chinese fantasy wand-management UI concept image.
Input image: first-pass.png is the edit target. It is this direction's independently generated first pass, not an external or historical design reference.

PRIMARY REQUEST: Correct ONLY the two short Chinese row labels immediately LEFT of the 2-row by 5-column grid under the heading “范围锚点 F3”. The current TOP row label is “前”; replace it with “后排”. The current BOTTOM row label is “后”; replace it with “前排”. This corrects the meaning of F3: F is the FRONT row and is the BOTTOM row of the schematic. Fit the corrected Chinese text in the existing blank space to the left of each row, matching the same dark ink, Chinese type style, scale, baseline alignment and paper texture.

INVARIANTS — preserve everything else as closely as possible:
- Preserve the entire composition, proportions, 3:4 portrait aspect ratio, crop, material rendering, objects, controls and all other text.
- Preserve the title “范围锚点 F3”.
- Preserve EXACTLY 2 rows × 5 columns, ten cells total.
- Preserve the upper row lighting: only the third cell lit green.
- Preserve the lower row lighting: only the second, third and fourth cells lit green.
- Preserve the white triangular anchor inside the LOWER row THIRD cell.
- Thus preserve exactly four green cells and the same T-shaped coverage.
- Preserve four deployed wands in the unfolded leather bag, their order and spell labels; preserve two reserve plain wands on the independent rack; preserve the selected rhythm-wand preview on the specimen sheet.
- Preserve exactly two empty inlay slots, the fixed “节律芯”, type “简易”, all cooldown/start/release values, timing summary, return-map control, unsaved state and the save-configuration action.
- Do not redraw, reinterpret, simplify, redesign or add any feature. Do not add new text, badges, panels, decorative objects or an explanation of the correction.

Only visible difference requested: top row says “后排”, bottom row says “前排”. Keep the rest of this specific image unchanged.
