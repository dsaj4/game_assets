Use case: text-localization
Asset type: a precise functional-label correction to the newly generated Chinese wand-management game UI.
Input image role: the sole referenced image is my own first draft generated in this task, the edit target. It is not a historical design or style reference.

Change only TWO small Chinese row labels beside the 2×5 range diagram. In the current image, the TOP row label incorrectly reads “前排”; replace that label with “后排”. The BOTTOM row label incorrectly reads “后排”; replace that label with “前排”. Correct final meaning is: TOP = 后排 / back; BOTTOM = 前排 / front. Only write these two Chinese labels, do not add the English explanations.

Preserve everything else from the input image with maximum precision: the 3:4 portrait format, full page composition, black/bone-white/cinnabar woodcut style, exactly four deployed wands in the leather bag, exactly two reserve raw wands on the independent rack, every existing Chinese heading and spell label, the selected rhythm wand, fixed core, exactly two empty inlay sockets, the cooldown-start ruler, release summary, unsaved state, return-to-map control and save-configuration control. Do not change any layout, color, size, illustration or other word.

The range itself is already correct and MUST NOT CHANGE: exactly 2 rows × 5 columns. TOP row lit cells: column 3 only. BOTTOM row lit cells: columns 2, 3 and 4. White anchor dot stays at the BOTTOM row, column 3. “范围锚点 F3” remains unchanged. Do not swap the cell contents or move the anchor; only swap the two Chinese row labels.
